#include <iostream>
#include <algorithm>
#include <execution>
#include <vector>
#include <random>
#include <string>
using namespace std;

int main()
{
	// size_t n;
	// cin >> n;
	// vector<int> a(n);
	// random_device rd;
	// mt19937 rng(rd());
	// std::generate(a.begin(), a.end(), rng);
	// cout << *std::max_element(std::execution::par_unseq, a.begin(), a.end()) << endl;
	// std::ranges::generate(a, rng);
	// cout << *std::ranges::max_element(a) << endl;

	// int shit[4] = {3850418404, 2766514333, 3216958631};
	// char str[] = {'a', 'b', 'c', 'd', 'e', 'f', 'g', 'h'};
	// cout << str << endl;

	string s = "sadl\n"
			   "sdaklj\n"
			   "asdkllk\n";
	cout << s << endl;

	return 0;
}
