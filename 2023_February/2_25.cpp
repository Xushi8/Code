#include<bits/stdc++.h>
using namespace std;


int main()
{
    // vector<int> ans;
    // unordered_map<int, int> umap;
    // for (int i = 0; i < nums.size(); i++)
    // {
    //     if (umap.find(target - nums[i]) != umap.end())
    //     {
    //         ans.push_back(i);
    //         ans.push_back(umap.find(target - nums[i])->second);
    //         return ans;
    //     }
        
    //     umap.insert(pair<int, int>(nums[i], i));
    // }

    system("pause");
    return 0;
}